import { useState, useEffect, useRef } from "react";

const STEPS = [
  {
    num: "01",
    label: "Describe",
    desc: "Write your domain in plain English — tables, relationships, business rules.",
    icon: (
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8">
        <path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/>
      </svg>
    ),
  },
  {
    num: "02",
    label: "Review Logical Model",
    desc: "AI generates entities and relationships. Edit, rename, or restructure before proceeding.",
    icon: (
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8">
        <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
      </svg>
    ),
  },
  {
    num: "03",
    label: "Physical Model",
    desc: "Columns, data types, indexes and constraints are resolved for your target engine.",
    icon: (
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8">
        <ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5v14a9 3 0 0 0 18 0V5"/><path d="M3 12a9 3 0 0 0 18 0"/>
      </svg>
    ),
  },
  {
    num: "04",
    label: "Generate SQL",
    desc: "Production-ready DDL for MySQL, PostgreSQL, Snowflake and more — validated, clean.",
    icon: (
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8">
        <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
      </svg>
    ),
  },
  {
    num: "05",
    label: "Export ERD",
    desc: "Visual entity-relationship diagram plus SAP PowerDesigner PDM/LDM files.",
    icon: (
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8">
        <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/>
        <path d="M10 6.5h4M17.5 10v4M10 17.5h4M6.5 10v4"/>
      </svg>
    ),
  },
];

const ENGINES = ["MySQL", "PostgreSQL", "Snowflake", "SQL Server", "Oracle", "BigQuery"];

const FEATURES = [
  {
    title: "RAG-Powered Domain Knowledge",
    body: "Azure AI Search grounds every generation in your enterprise knowledge base — not just generic patterns.",
    accent: "#ffe600",
  },
  {
    title: "Logical → Physical Pipeline",
    body: "Two-stage generation keeps business semantics separate from physical constraints. Approve each layer independently.",
    accent: "#ffe600",
  },
  {
    title: "Iterative Feedback Loop",
    body: "Reject, annotate, and re-generate at any step. The model learns from your corrections inline.",
    accent: "#ffe600",
  },
  {
    title: "SAP PowerDesigner Export",
    body: "First-class PDM and LDM output with symbols, associations and multiplicity — ready to open in PowerDesigner.",
    accent: "#ffe600",
  },
  {
    title: "Multi-Engine DDL",
    body: "Correct syntax, native data types and dialect-specific partitioning for six major database engines.",
    accent: "#ffe600",
  },
  {
    title: "Validation Built-In",
    body: "Auto-validation catches constraint conflicts, naming issues and type mismatches before you ever run the DDL.",
    accent: "#ffe600",
  },
];

function useInView(threshold = 0.15) {
  const ref = useRef(null);
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) setVisible(true); }, { threshold });
    if (ref.current) obs.observe(ref.current);
    return () => obs.disconnect();
  }, []);
  return [ref, visible];
}

function AnimatedCounter({ target, suffix = "" }) {
  const [count, setCount] = useState(0);
  const [ref, visible] = useInView(0.5);
  useEffect(() => {
    if (!visible) return;
    let start = 0;
    const step = Math.ceil(target / 40);
    const id = setInterval(() => {
      start += step;
      if (start >= target) { setCount(target); clearInterval(id); }
      else setCount(start);
    }, 30);
    return () => clearInterval(id);
  }, [visible, target]);
  return <span ref={ref}>{count}{suffix}</span>;
}

export function LandingPage({ onGetStarted }) {
  const [activeStep, setActiveStep] = useState(0);
  const [heroVisible, setHeroVisible] = useState(false);
  const [engineIdx, setEngineIdx] = useState(0);

  useEffect(() => {
    const t = setTimeout(() => setHeroVisible(true), 80);
    return () => clearTimeout(t);
  }, []);

  useEffect(() => {
    const id = setInterval(() => setEngineIdx(i => (i + 1) % ENGINES.length), 1800);
    return () => clearInterval(id);
  }, []);

  const [featRef, featVisible] = useInView();
  const [stepsRef, stepsVisible] = useInView();
  const [statsRef, statsVisible] = useInView();

  return (
    <div style={{
      background: "#0f0f13",
      minHeight: "100vh",
      fontFamily: "'DM Sans', 'Segoe UI', system-ui, sans-serif",
      color: "#e8e8f0",
      overflowX: "hidden",
    }}>
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&family=Syne:wght@700;800&display=swap');

        * { box-sizing: border-box; margin: 0; padding: 0; }

        .ey-btn {
          display: inline-flex; align-items: center; gap: 10px;
          background: #ffe600; color: #0f0f13;
          font-family: inherit; font-weight: 700; font-size: 1rem;
          padding: 14px 32px; border: none; cursor: pointer;
          transition: all 0.2s ease; letter-spacing: 0.01em;
          clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px));
        }
        .ey-btn:hover { background: #fff176; transform: translateY(-2px); box-shadow: 0 8px 32px rgba(255,230,0,0.25); }

        .ey-btn-ghost {
          display: inline-flex; align-items: center; gap: 8px;
          background: transparent; color: #e8e8f0;
          font-family: inherit; font-weight: 500; font-size: 0.95rem;
          padding: 13px 28px; border: 1.5px solid rgba(255,255,255,0.2); cursor: pointer;
          transition: all 0.2s ease;
        }
        .ey-btn-ghost:hover { border-color: #ffe600; color: #ffe600; }

        .step-pill {
          cursor: pointer;
          border: 1px solid rgba(255,230,0,0.15);
          background: rgba(255,230,0,0.04);
          transition: all 0.25s ease;
          padding: 14px 18px;
        }
        .step-pill:hover, .step-pill.active {
          border-color: rgba(255,230,0,0.5);
          background: rgba(255,230,0,0.09);
        }
        .step-pill.active .step-num { color: #ffe600; }

        .feat-card {
          border: 1px solid rgba(255,255,255,0.07);
          background: rgba(255,255,255,0.03);
          padding: 28px;
          transition: all 0.3s ease;
          position: relative;
          overflow: hidden;
        }
        .feat-card::before {
          content: ''; position: absolute; top: 0; left: 0;
          width: 3px; height: 0;
          background: #ffe600;
          transition: height 0.35s ease;
        }
        .feat-card:hover::before { height: 100%; }
        .feat-card:hover {
          border-color: rgba(255,230,0,0.2);
          background: rgba(255,230,0,0.04);
          transform: translateY(-3px);
        }

        .engine-tag {
          display: inline-block;
          padding: 3px 10px;
          border: 1px solid rgba(255,230,0,0.3);
          font-size: 0.78rem;
          font-weight: 600;
          color: #ffe600;
          letter-spacing: 0.06em;
          text-transform: uppercase;
        }

        .grid-bg {
          background-image:
            linear-gradient(rgba(255,230,0,0.03) 1px, transparent 1px),
            linear-gradient(90deg, rgba(255,230,0,0.03) 1px, transparent 1px);
          background-size: 48px 48px;
        }

        .noise {
          position: absolute; inset: 0; pointer-events: none; z-index: 0;
          background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
          opacity: 0.5;
        }

        .fade-up {
          opacity: 0; transform: translateY(28px);
          transition: opacity 0.6s ease, transform 0.6s ease;
        }
        .fade-up.visible { opacity: 1; transform: translateY(0); }
        .fade-up.d1 { transition-delay: 0.1s; }
        .fade-up.d2 { transition-delay: 0.2s; }
        .fade-up.d3 { transition-delay: 0.3s; }
        .fade-up.d4 { transition-delay: 0.4s; }
        .fade-up.d5 { transition-delay: 0.5s; }
        .fade-up.d6 { transition-delay: 0.6s; }

        .glow-line {
          height: 1px;
          background: linear-gradient(90deg, transparent, #ffe600, transparent);
          opacity: 0.4;
        }

        .stat-box {
          border: 1px solid rgba(255,230,0,0.15);
          padding: 32px 24px;
          text-align: center;
          background: rgba(255,230,0,0.03);
        }

        @media (max-width: 768px) {
          .hero-title { font-size: 2.8rem !important; }
          .hide-mobile { display: none !important; }
          .cols-2 { grid-template-columns: 1fr !important; }
          .cols-3 { grid-template-columns: 1fr !important; }
        }
      `}</style>

      {/* ── NAV ── */}
      <nav style={{
        position: "sticky", top: 0, zIndex: 100,
        display: "flex", alignItems: "center", justifyContent: "space-between",
        padding: "0 48px", height: "64px",
        background: "rgba(15,15,19,0.92)",
        backdropFilter: "blur(12px)",
        borderBottom: "1px solid rgba(255,255,255,0.07)",
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
          <div style={{
            width: 28, height: 28,
            background: "#ffe600",
            clipPath: "polygon(0 0, 80% 0, 100% 20%, 100% 100%, 20% 100%, 0 80%)",
          }} />
          <span style={{ fontFamily: "'Syne', sans-serif", fontWeight: 800, fontSize: "1.05rem", color: "#fff", letterSpacing: "-0.01em" }}>
            DataForge <span style={{ color: "#ffe600" }}>AI</span>
          </span>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
          <button className="ey-btn-ghost hide-mobile" style={{ fontSize: "0.9rem", padding: "9px 20px" }}>
            Docs
          </button>
          <button className="ey-btn" style={{ padding: "10px 22px", fontSize: "0.9rem" }} onClick={onGetStarted}>
            Launch App
          </button>
        </div>
      </nav>

      {/* ── HERO ── */}
      <section style={{ position: "relative", overflow: "hidden" }} className="grid-bg">
        <div className="noise" />

        {/* Yellow accent blob */}
        <div style={{
          position: "absolute", top: "-120px", right: "-80px",
          width: "560px", height: "560px",
          background: "radial-gradient(circle, rgba(255,230,0,0.12) 0%, transparent 70%)",
          pointerEvents: "none",
        }} />
        <div style={{
          position: "absolute", bottom: "40px", left: "-100px",
          width: "400px", height: "400px",
          background: "radial-gradient(circle, rgba(255,230,0,0.06) 0%, transparent 70%)",
          pointerEvents: "none",
        }} />

        <div style={{
          position: "relative", zIndex: 1,
          maxWidth: "1100px", margin: "0 auto",
          padding: "100px 48px 80px",
        }}>
          {/* Tag */}
          <div style={{
            opacity: heroVisible ? 1 : 0,
            transform: heroVisible ? "translateY(0)" : "translateY(16px)",
            transition: "all 0.5s ease",
            display: "inline-flex", alignItems: "center", gap: 8,
            border: "1px solid rgba(255,230,0,0.3)",
            padding: "5px 14px 5px 8px",
            marginBottom: "28px",
          }}>
            <div style={{ width: 6, height: 6, background: "#ffe600", borderRadius: "50%" }} />
            <span style={{ fontSize: "0.8rem", fontWeight: 600, color: "#ffe600", letterSpacing: "0.08em", textTransform: "uppercase" }}>
              EY · Data Modelling Suite
            </span>
          </div>

          {/* Headline */}
          <h1 className="hero-title" style={{
            fontFamily: "'Syne', sans-serif",
            fontWeight: 800,
            fontSize: "clamp(3rem, 6vw, 5.2rem)",
            lineHeight: 1.0,
            color: "#fff",
            letterSpacing: "-0.03em",
            marginBottom: "24px",
            opacity: heroVisible ? 1 : 0,
            transform: heroVisible ? "translateY(0)" : "translateY(24px)",
            transition: "all 0.6s ease 0.1s",
          }}>
            SQL schemas,<br />
            <span style={{
              color: "#ffe600",
              WebkitTextStroke: "0px",
              position: "relative",
            }}>
              built by AI.
            </span>
            <br />
            <span style={{ color: "rgba(255,255,255,0.35)", fontSize: "0.65em", fontWeight: 700 }}>
              Reviewed by you.
            </span>
          </h1>

          {/* Sub */}
          <p style={{
            fontSize: "1.15rem", color: "rgba(255,255,255,0.55)", lineHeight: 1.7,
            maxWidth: "540px", marginBottom: "40px",
            opacity: heroVisible ? 1 : 0,
            transform: heroVisible ? "translateY(0)" : "translateY(20px)",
            transition: "all 0.6s ease 0.2s",
          }}>
            Describe your domain. Get a production-ready DDL for{" "}
            <span className="engine-tag" style={{ transition: "all 0.3s" }}>
              {ENGINES[engineIdx]}
            </span>
            {" "}— with ERD and PowerDesigner exports included.
          </p>

          {/* CTA row */}
          <div style={{
            display: "flex", alignItems: "center", gap: 16, flexWrap: "wrap",
            opacity: heroVisible ? 1 : 0,
            transform: heroVisible ? "translateY(0)" : "translateY(16px)",
            transition: "all 0.6s ease 0.3s",
          }}>
            <button className="ey-btn" onClick={onGetStarted}>
              Start modelling
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </button>
            <button className="ey-btn-ghost">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <polygon points="5 3 19 12 5 21 5 3"/>
              </svg>
              Watch demo
            </button>
          </div>

          {/* Stats strip */}
          <div ref={statsRef} style={{
            display: "grid", gridTemplateColumns: "repeat(3, 1fr)",
            gap: 1, marginTop: "72px",
            background: "rgba(255,255,255,0.06)",
          }}>
            {[
              { val: 6, suffix: "+", label: "DB Engines" },
              { val: 5, suffix: " steps", label: "End-to-end pipeline" },
              { val: 100, suffix: "%", label: "DDL validated" },
            ].map((s, i) => (
              <div key={i} className="stat-box">
                <div style={{
                  fontFamily: "'Syne', sans-serif", fontWeight: 800,
                  fontSize: "2.4rem", color: "#ffe600", lineHeight: 1,
                  marginBottom: "6px",
                }}>
                  {statsVisible ? <AnimatedCounter target={s.val} suffix={s.suffix} /> : `0${s.suffix}`}
                </div>
                <div style={{ fontSize: "0.82rem", color: "rgba(255,255,255,0.4)", fontWeight: 500, textTransform: "uppercase", letterSpacing: "0.06em" }}>
                  {s.label}
                </div>
              </div>
            ))}
          </div>
        </div>

        <div className="glow-line" />
      </section>

      {/* ── PIPELINE STEPS ── */}
      <section style={{ padding: "96px 48px", maxWidth: "1100px", margin: "0 auto" }}>
        <div ref={stepsRef}>
          <div className={`fade-up ${stepsVisible ? "visible" : ""}`}>
            <div style={{ display: "flex", alignItems: "center", gap: 16, marginBottom: "12px" }}>
              <div style={{ width: 32, height: 2, background: "#ffe600" }} />
              <span style={{ fontSize: "0.78rem", fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "#ffe600" }}>
                How it works
              </span>
            </div>
            <h2 style={{
              fontFamily: "'Syne', sans-serif", fontWeight: 800,
              fontSize: "clamp(1.8rem, 3.5vw, 2.8rem)",
              color: "#fff", letterSpacing: "-0.025em",
              marginBottom: "48px",
            }}>
              Five steps from idea<br />to production schema.
            </h2>
          </div>

          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 2 }} className="cols-2">
            {/* Step selector */}
            <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
              {STEPS.map((s, i) => (
                <div
                  key={i}
                  className={`step-pill fade-up ${stepsVisible ? "visible" : ""} d${i + 1} ${activeStep === i ? "active" : ""}`}
                  onClick={() => setActiveStep(i)}
                >
                  <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
                    <span className="step-num" style={{
                      fontFamily: "'Syne', sans-serif", fontWeight: 800,
                      fontSize: "0.78rem", color: "rgba(255,255,255,0.25)",
                      letterSpacing: "0.06em", minWidth: "24px",
                      transition: "color 0.25s",
                    }}>
                      {s.num}
                    </span>
                    <div style={{ color: activeStep === i ? "#ffe600" : "rgba(255,255,255,0.4)", transition: "color 0.25s" }}>
                      {s.icon}
                    </div>
                    <span style={{
                      fontWeight: 600, fontSize: "0.95rem",
                      color: activeStep === i ? "#fff" : "rgba(255,255,255,0.6)",
                      transition: "color 0.25s",
                    }}>
                      {s.label}
                    </span>
                  </div>
                </div>
              ))}
            </div>

            {/* Step detail */}
            <div style={{
              border: "1px solid rgba(255,230,0,0.2)",
              background: "rgba(255,230,0,0.04)",
              padding: "40px",
              display: "flex", flexDirection: "column", justifyContent: "center",
              position: "relative", overflow: "hidden",
            }}>
              <div style={{
                position: "absolute", top: 0, right: 0,
                fontFamily: "'Syne', sans-serif", fontWeight: 800,
                fontSize: "8rem", lineHeight: 1,
                color: "rgba(255,230,0,0.05)",
                pointerEvents: "none", userSelect: "none",
              }}>
                {STEPS[activeStep].num}
              </div>
              <div style={{ color: "#ffe600", marginBottom: "16px" }}>
                {STEPS[activeStep].icon}
              </div>
              <h3 style={{
                fontFamily: "'Syne', sans-serif", fontWeight: 800,
                fontSize: "1.5rem", color: "#fff",
                letterSpacing: "-0.02em", marginBottom: "14px",
              }}>
                {STEPS[activeStep].label}
              </h3>
              <p style={{ color: "rgba(255,255,255,0.55)", lineHeight: 1.8, fontSize: "1rem" }}>
                {STEPS[activeStep].desc}
              </p>
              <div style={{ marginTop: "28px", display: "flex", alignItems: "center", gap: 8 }}>
                {STEPS.map((_, i) => (
                  <div key={i} onClick={() => setActiveStep(i)} style={{
                    width: activeStep === i ? 24 : 6,
                    height: 3,
                    background: activeStep === i ? "#ffe600" : "rgba(255,255,255,0.15)",
                    cursor: "pointer",
                    transition: "all 0.3s ease",
                  }} />
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      <div className="glow-line" />

      {/* ── FEATURES ── */}
      <section style={{ padding: "96px 48px", maxWidth: "1100px", margin: "0 auto" }}>
        <div ref={featRef}>
          <div className={`fade-up ${featVisible ? "visible" : ""}`} style={{ marginBottom: "56px" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 16, marginBottom: "12px" }}>
              <div style={{ width: 32, height: 2, background: "#ffe600" }} />
              <span style={{ fontSize: "0.78rem", fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "#ffe600" }}>
                Capabilities
              </span>
            </div>
            <h2 style={{
              fontFamily: "'Syne', sans-serif", fontWeight: 800,
              fontSize: "clamp(1.8rem, 3.5vw, 2.8rem)",
              color: "#fff", letterSpacing: "-0.025em",
            }}>
              Everything you need to go<br />from concept to DDL.
            </h2>
          </div>

          <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 2 }} className="cols-3">
            {FEATURES.map((f, i) => (
              <div
                key={i}
                className={`feat-card fade-up ${featVisible ? "visible" : ""} d${i + 1}`}
              >
                <h4 style={{
                  fontWeight: 700, fontSize: "1rem",
                  color: "#fff", marginBottom: "10px", letterSpacing: "-0.01em",
                }}>
                  {f.title}
                </h4>
                <p style={{ fontSize: "0.9rem", color: "rgba(255,255,255,0.45)", lineHeight: 1.75 }}>
                  {f.body}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <div className="glow-line" />

      {/* ── CTA BAND ── */}
      <section style={{
        position: "relative", overflow: "hidden",
        background: "#ffe600",
        padding: "80px 48px",
        textAlign: "center",
      }}>
        <div style={{
          position: "absolute", inset: 0,
          background: "repeating-linear-gradient(45deg, transparent, transparent 20px, rgba(0,0,0,0.03) 20px, rgba(0,0,0,0.03) 21px)",
          pointerEvents: "none",
        }} />
        <div style={{ position: "relative", zIndex: 1 }}>
          <h2 style={{
            fontFamily: "'Syne', sans-serif", fontWeight: 800,
            fontSize: "clamp(2rem, 4vw, 3.2rem)",
            color: "#0f0f13", letterSpacing: "-0.03em",
            marginBottom: "16px",
          }}>
            Ready to model your first schema?
          </h2>
          <p style={{ color: "rgba(0,0,0,0.5)", fontSize: "1.05rem", marginBottom: "36px" }}>
            No setup. No credentials. Just describe your domain and go.
          </p>
          <button
            onClick={onGetStarted}
            style={{
              display: "inline-flex", alignItems: "center", gap: 10,
              background: "#0f0f13", color: "#ffe600",
              fontFamily: "inherit", fontWeight: 700, fontSize: "1rem",
              padding: "15px 36px", border: "none", cursor: "pointer",
              transition: "all 0.2s ease",
              clipPath: "polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px))",
            }}
            onMouseOver={e => { e.currentTarget.style.background = "#1a1a22"; e.currentTarget.style.transform = "translateY(-2px)"; }}
            onMouseOut={e => { e.currentTarget.style.background = "#0f0f13"; e.currentTarget.style.transform = "translateY(0)"; }}
          >
            Start modelling free
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </button>
        </div>
      </section>

      {/* ── FOOTER ── */}
      <footer style={{
        borderTop: "1px solid rgba(255,255,255,0.06)",
        padding: "28px 48px",
        display: "flex", alignItems: "center", justifyContent: "space-between",
        flexWrap: "wrap", gap: 16,
        background: "#0c0c10",
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
          <div style={{
            width: 20, height: 20,
            background: "#ffe600",
            clipPath: "polygon(0 0, 80% 0, 100% 20%, 100% 100%, 20% 100%, 0 80%)",
          }} />
          <span style={{ fontFamily: "'Syne', sans-serif", fontWeight: 800, fontSize: "0.9rem", color: "rgba(255,255,255,0.4)" }}>
            DataForge AI · EY
          </span>
        </div>
        <span style={{ fontSize: "0.82rem", color: "rgba(255,255,255,0.2)" }}>
          Built for enterprise data modelling
        </span>
      </footer>
    </div>
  );
}
